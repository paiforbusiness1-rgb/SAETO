# Copia a backend/.env o exporta en la shell antes de uvicorn
GROQ_API_KEY=

# Opcional en Vercel (si no se define, cae a /tmp/saeto-runtime cuando el FS es read-only)
# SAETO_RUNTIME_DIR=/tmp/saeto-runtime
